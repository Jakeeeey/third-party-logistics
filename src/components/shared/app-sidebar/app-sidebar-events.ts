/**
 * Global event name for refreshing sidebars across different subsystems.
 * Used by client components to trigger a server-side data re-fetch.
 */
export const APP_SIDEBAR_REFRESH_EVENT = "vos_sidebar_refresh";
